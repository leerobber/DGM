[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "dgm-rs"
version = "0.1.0"
description = "DGM — Rust graph engine with pyo3 Python bindings"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "dgm_rs"
