# DGM — Sovereign Core edition
# Zero cloud API dependencies. All LLM calls route to local vLLM endpoint.

# OpenAI-compatible client (used against local vLLM/litellm endpoint)
openai>=1.12.0

# Retry with exponential back-off
backoff

# SWE-Bench evaluation
beautifulsoup4
chardet
docker
ghapi
GitPython
pre-commit
python-dotenv
rich
unidiff

# Dataset loading
datasets

# Testing
pytest
pytest-asyncio
async_timeout
