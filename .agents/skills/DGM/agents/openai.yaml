interface:
  display_name: "DGM"
  short_description: "Repo-specific patterns and workflows for DGM"
  default_prompt: "Use the DGM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true